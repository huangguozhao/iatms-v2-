# 测试执行流程图

## 1. 单用例执行流程

```mermaid
flowchart TD
    subgraph 单用例执行
        A1(["开始"]) --> A2[Controller接收请求<br/>executeTestCase]
        A2 --> A3[executeTestCaseWithVariables<br/>接收参数]
        A3 --> A4{查询用例信息<br/>TestExecutionMapper.findTestCaseForExecution}
        A4 -->|用例不存在| A5[抛出异常]
        A5 --> A20([结束])
        
        A4 -->|用例存在| A6[创建执行记录<br/>TestExecutionRecord]
        A6 --> A7[设置执行参数<br/>setExecutionParameters]
        A7 --> A8[应用变量池<br/>applyVariablesFromPool]
        A8 --> A9[TestCaseExecutor.executeTestCase<br/>执行HTTP请求]
        A9 --> A10[生成执行ID<br/>generateExecutionId]
        A10 --> A11[保存测试结果<br/>TestCaseResult]
        A11 --> A12[生成测试报告<br/>generateTestReport]
        A12 --> A13[更新执行记录<br/>updateExecutionRecord]
        A13 --> A14[返回ExecutionResultDTO]
        A14 --> A15([结束])
    end

    style A1 fill:#e1f5fe
    style A5 fill:#ffcdd2
    style A15 fill:#c8e6c9
```

## 2. 批量执行流程（串行）

```mermaid
flowchart TD
    subgraph 批量执行-串行
        B1(["开始"]) --> B2[Controller接收请求<br/>executeTestCases]
        B2 --> B3[创建批量执行记录<br/>TestExecutionRecord]
        B3 --> B4[初始化统计变量<br/>passed=0, failed=0]
        B4 --> B5{for each caseId in caseIds}
        
        B5 -->|遍历| B6{查询用例信息<br/>findTestCaseForExecution}
        B6 -->|用例不存在| B7[skipped++]
        B7 --> B5
        
        B6 -->|用例存在| B8[executeTestCaseInternal<br/>执行单个用例]
        B8 --> B9{判断执行结果}
        B9 -->|passed| B10[passed++]
        B10 --> B5
        B9 -->|failed| B11[failed++]
        B11 --> B5
        B9 -->|skipped| B12[skipped++]
        B12 --> B5
        
        B5 -->|遍历完成| B13[更新执行记录统计<br/>passed/failed/skipped]
        B13 --> B14[生成汇总报告<br/>generateBatchTestReport]
        B14 --> B15[返回ModuleExecutionResultDTO]
        B15 --> B16([结束])
    end

    style B1 fill:#e1f5fe
    style B16 fill:#c8e6c9
```

## 3. 并发执行流程

```mermaid
flowchart TD
    subgraph 模块级并发执行
        C1(["开始"]) --> C2[executeTestCasesSync<br/>接收模块和用例列表]
        C2 --> C3[初始化变量池<br/>variablePoolService.initializePool]
        C3 --> C4[依赖排序<br/>dependencyResolverService.sortTestCasesByDependency]
        C4 --> C5[获取执行层级<br/>getExecutionLayers]
        C5 --> C6[创建执行记录<br/>TestExecutionRecord]
        C6 --> C7[创建测试报告<br/>createModuleTestReportSummary]
        C7 --> C8{for each layer in executionLayers}
        
        C8 -->|每层| C9[创建CompletableFuture列表]
        C9 --> C10{for each testCase in layer}
        
        C10 -->|每个用例| C11[CompletableFuture.runAsync<br/>提交到线程池executorService]
        C11 --> C12[executeTestCaseWithVariables<br/>执行用例]
        C12 --> C13{执行成功?}
        C13 -->|是| C14[收集结果 + 提取变量<br/>variablePoolService.setVariables]
        C14 --> C10
        C13 -->|否| C15[记录失败结果]
        C15 --> C10
        
        C10 --> C16[等待本层执行完成<br/>CompletableFuture.allOf.join]
        C16 --> C8
        
        C8 -->|所有层完成| C17[清理变量池<br/>variablePoolService.clearPool]
        C17 --> C18[计算统计结果<br/>passed/failed/skipped/broken]
        C18 --> C19[更新报告统计<br/>TestReportSummary]
        C19 --> C20[更新执行记录<br/>TestExecutionRecord]
        C20 --> C21[返回ModuleExecutionResultDTO]
        C21 --> C22([结束])
    end

    style C1 fill:#e1f5fe
    style C22 fill:#c8e6c9
```

## 4. 核心组件说明

```mermaid
flowchart TD
    subgraph 核心组件
        D1[TestExecutionService]
        D2[TestCaseExecutor]
        D3[ExecutorService<br/>线程池-20线程]
        D4[TestExecutionMapper]
        D5[TestExecutionRecordMapper]
        D6[VariablePoolService]
        D7[DependencyResolverService]
    end

    D1 --> D2
    D1 --> D3
    D1 --> D4
    D1 --> D5
    D3 --> D2
    D2 --> D4
    D6 --> D1
    D7 --> D1
```

## 流程说明

### 单用例执行
1. Controller 接收请求，调用 `executeTestCase()`
2. 查询用例信息，验证用例存在且启用
3. 创建执行记录 `TestExecutionRecord`
4. 调用 `TestCaseExecutor.executeTestCase()` 执行 HTTP 请求
5. 保存测试结果 `TestCaseResult`
6. 生成测试报告
7. 返回执行结果

### 批量执行（串行）
1. 接收用例 ID 列表
2. 创建批量执行记录
3. 遍历每个用例，串行调用 `executeTestCaseInternal()`
4. 统计通过/失败/跳过数量
5. 生成汇总报告
6. 返回结果

### 并发执行
1. 接收模块和用例列表
2. 通过依赖解析服务对用例排序，划分执行层级
3. 使用 `CompletableFuture.runAsync()` 提交到 20 线程的线程池
4. 按层级顺序执行（层内并发，层间串行）
5. 共享变量池存储提取的变量供依赖用例使用
6. 汇总统计结果，生成报告