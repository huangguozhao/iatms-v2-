# 用户权限管理模块设计流程图

## 1. 整体权限架构

```mermaid
flowchart TD
    subgraph 权限架构
        A1[用户请求] --> A2[Controller方法]
        A2 --> A3["@GlobalInterceptor注解"]
        A3 --> A4[GlobalOperationAspect AOP切面]
        A4 --> A5{JWT令牌校验}
        
        A5 -->|无效/无令牌| A6[返回401未授权]
        A5 -->|有效| A7{权限类型校验}
        
        A7 -->|checkLogin| A8[验证登录状态]
        A7 -->|checkAdmin| A9[验证管理员角色]
        A7 -->|checkPermission| A10[验证系统权限]
        A7 -->|checkProjectPermission| A11[验证项目权限]
        A7 -->|checkExecutePermission| A12[验证执行权限]
        
        A8 --> A13[设置userId到request]
        A9 --> A13
        A10 --> A13
        A11 --> A13
        A12 --> A13
        
        A13 --> A14[执行业务逻辑]
    end

    style A1 fill:#e1f5fe
    style A6 fill:#ffcdd2
    style A14 fill:#c8e6c9
```

## 2. JWT认证流程

```mermaid
flowchart TD
    subgraph 用户登录
        B1(["用户登录"]) --> B2[AuthController.login]
        B2 --> B3[UserService验证用户名密码]
        B3 --> B4{验证成功?}
        
        B4 -->|否| B5[返回错误]
        B4 -->|是| B6[jwtUtils.generateToken<br/>生成JWT令牌]
        
        B6 --> B7[返回token给客户端]
        B7 --> B8([登录成功])
    end
    
    subgraph 请求验证
        B9(["后续请求"]) --> B10[获取Header中的Authorization]
        B10 --> B11{是否有Bearer Token?}
        
        B11 -->|否| B12[返回901未登录错误]
        B11 -->|是| B13[提取Token字符串]
        
        B13 --> B14[jwtUtils.validateToken<br/>验证Token有效性]
        B14 --> B15{Token有效?}
        
        B15 -->|否| B16[返回901未登录错误]
        B15 --> B17[jwtUtils.getUserIdFromToken<br/>获取用户ID]
        
        B17 --> B18[设置userId到request属性]
        B18 --> B19[执行业务逻辑]
    end

    style B1 fill:#e1f5fe
    style B8 fill:#c8e6c9
    style B12 fill:#ffcdd2
    style B16 fill:#ffcdd2
```

## 3. GlobalInterceptor注解校验流程

```mermaid
flowchart TD
    subgraph 注解拦截
        C1[请求进入Controller] --> C2["获取方法上的@GlobalInterceptor注解"]
        C2 --> C3{检查需要哪些校验}
        
        C3 --> C4{checkLogin=true?}
        C4 -->|是| C5[checkLogin校验]
        C4 -->|否| C6
        
        C5 --> C6{checkAdmin=true?}
        C6 -->|是| C7[checkAdmin校验]
        C6 -->|否| C8
        
        C7 --> C8{checkPermission数组<br/>长度>0?}
        C8 -->|是| C9[checkPermission校验]
        C8 -->|否| C10
        
        C9 --> C10{checkProjectPermission<br/>非空?}
        C10 -->|是| C11[checkProjectPermission校验]
        C10 -->|否| C12
        
        C11 --> C12{checkExecutePermission=true?}
        C12 -->|是| C13[checkExecutePermission校验]
        C12 -->|否| C14[放行执行业务]
        
        C5 --> C15[返回错误]
        C7 --> C15
        C9 --> C15
        C11 --> C15
        C13 --> C15
    end

    style C1 fill:#e1f5fe
    style C14 fill:#c8e6c9
    style C15 fill:#ffcdd2
```

## 4. 项目权限校验流程（hasProjectPermission）

```mermaid
flowchart TD
    subgraph 项目权限校验
        D1[hasProjectPermission<br/>userId/projectId/permission] --> D2{参数为空?}
        
        D2 -->|是| D3[返回false]
        D2 -->|否| D4[查询用户信息]
        
        D4 --> D5{用户存在且状态为active?}
        D5 -->|否| D3
        D5 -->|是| D6[isAdmin检查]
        
        D6 --> D7{是管理员?}
        D7 -->|是| D8[返回true<br/>管理员拥有所有权限]
        D7 -->|否| D9[查询项目成员信息]
        
        D9 --> D10{是项目成员且状态active?}
        D10 -->|否| D3
        D10 -->|是| D11[获取projectRole]
        
        D11 --> D12[checkProjectRolePermission<br/>根据角色判断权限]
        D12 --> D13[返回权限校验结果]
    end

    style D1 fill:#e1f5fe
    style D3 fill:#ffcdd2
    style D8 fill:#c8e6c9
    style D13 fill:#c8e6c9
```

## 5. 项目角色权限矩阵

```mermaid
flowchart TD
    subgraph 角色权限判断
        E1[checkProjectRolePermission<br/>projectRole/permission] --> E2{权限类型}
        
        E2 -->|以:view结尾| E3[所有角色都可查看<br/>返回true]
        
        E2 -->|其他权限| E4{projectRole角色}
        
        E4 -->|owner| E5[返回true<br/>拥有所有权限]
        E4 -->|manager| E6{是project:delete?}
        E6 -->|是| E7[返回false]
        E6 -->|否| E8[返回true]
        
        E4 -->|developer| E9{是project:xxx非view?}
        E9 -->|是| E10[返回false<br/>不能管理项目]
        E9 -->|否| E11{是project:manage_members?}
        E11 -->|是| E12[返回false]
        E11 -->|否| E13{是module:delete?}
        E13 -->|是| E14[返回false]
        E13 -->|否| E15[返回true]
        
        E4 -->|tester| E16{是project:xxx非view?}
        E16 -->|是| E17[返回false]
        E16 -->|否| E18{是manage_members或delete?}
        E18 -->|是| E19[返回false]
        E18 -->|否| E20[返回true]
        
        E4 -->|viewer| E21[返回false<br/>只能查看]
        
        E3 --> E22([返回结果])
        E5 --> E22
        E7 --> E22
        E8 --> E22
        E10 --> E22
        E12 --> E22
        E14 --> E22
        E15 --> E22
        E17 --> E22
        E19 --> E22
        E20 --> E22
        E21 --> E22
    end
```

## 6. 权限级别说明

```mermaid
flowchart TD
    subgraph 系统级角色
        F1[系统角色 - users表role字段]
        F2[admin - 管理员<br/>拥有全部系统权限]
        F3[user - 普通用户<br/>需通过项目成员获得权限]
    end
    
    subgraph 项目级权限
        F4[项目成员 - projectmembers表]
        F5[permission_level权限级别]
        F6[read - 只读]
        F7[write - 可编辑]
        F8[admin - 管理员]
    end
    
    subgraph 项目角色
        F9[project_role项目角色]
        F10[owner - 所有者]
        F11[manager - 管理者]
        F12[developer - 开发者]
        F13[tester - 测试人员]
        F14[viewer - 查看者]
    end
```

## 7. 核心组件关系

```mermaid
flowchart TD
    subgraph 核心组件
        G1[UserController<br/>用户管理]
        G2[ProjectController<br/>项目管理]
        G3[其他业务Controller]
    end
    
    subgraph 权限控制
        G4["@GlobalInterceptor注解"]
        G5[GlobalOperationAspect<br/>AOP切面]
        G6[JwtUtils<br/>JWT工具]
        G7[PermissionService<br/>权限服务]
    end
    
    subgraph 数据层
        G8[UserMapper<br/>用户表]
        G9[ProjectMemberMapper<br/>项目成员表]
        G10[users表<br/>role字段]
        G11[projectmembers表<br/>permission_level<br/>project_role]
    end
    
    G1 --> G4
    G2 --> G4
    G3 --> G4
    G4 --> G5
    G5 --> G6
    G5 --> G7
    G7 --> G8
    G7 --> G9
    G8 --> G10
    G9 --> G11
```

## 流程说明

### 1. 整体权限架构
1. 用户请求进入Controller方法
2. 方法上的`@GlobalInterceptor`注解触发AOP切面
3. 切面进行JWT令牌校验
4. 根据注解参数进行相应权限校验（登录/管理员/系统权限/项目权限/执行权限）
5. 校验通过后设置userId到request属性，执行业务逻辑

### 2. JWT认证流程
- **登录**：验证用户名密码成功后，调用`jwtUtils.generateToken()`生成JWT令牌返回给客户端
- **后续请求**：从Header中获取Authorization Bearer Token，调用`jwtUtils.validateToken()`验证有效性，有效则获取userId设置到request

### 3. GlobalInterceptor注解校验流程
- **checkLogin**：验证用户是否登录
- **checkAdmin**：验证是否是管理员
- **checkPermission**：验证系统权限
- **checkProjectPermission**：验证项目权限
- **checkExecutePermission**：验证执行权限

### 4. 项目权限校验流程
1. 接收userId/projectId/permission参数
2. 检查用户是否存在且状态为active
3. 管理员直接拥有所有权限
4. 查询项目成员信息
5. 根据projectRole调用`checkProjectRolePermission()`判断权限

### 5. 项目角色权限矩阵
- **owner**：拥有所有权限
- **manager**：除删除项目外所有权限
- **developer**：不能管理项目和成员，不能删除模块
- **tester**：不能删除项目/模块/接口/用例/任务
- **viewer**：只能查看