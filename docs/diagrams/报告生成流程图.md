# 报告生成流程图

## 1. 测试执行中报告生成流程

```mermaid
flowchart TD
    subgraph 执行中报告生成
        A1(["测试执行开始"]) --> A2{执行类型}
        
        A2 -->|单用例| A3[executeTestCaseWithVariables]
        A2 -->|批量/模块/项目| A4[createModuleTestReportSummary<br/>或createProjectTestReportSummary]
        
        A3 --> A5[executeTestCase执行HTTP请求]
        A5 --> A6[保存TestCaseResult]
        A6 --> A7{是否批量执行?}
        
        A7 -->|否-单独报告| A8[generateTestReport<br/>生成单用例报告]
        A8 --> A9[buildTestReportSummary<br/>构建报告摘要]
        A9 --> A10[insertTestReportSummary<br/>保存到数据库]
        
        A7 -->|是-共享报告| A11[不生成单独报告]
        
        A4 --> A12[创建报告摘要记录<br/>初始状态=generating]
        A12 --> A13[执行测试用例]
        A13 --> A14[recordModuleTestCaseResult<br/>记录用例结果]
        
        A14 --> A15{所有用例执行完成?}
        A15 -->|否| A13
        A15 -->|是| A16[updateReportSummaryStats<br/>更新报告统计]
        
        A16 --> A17[统计passed/failed/broken/skipped]
        A17 --> A18[更新TestReportSummary<br/>设置状态=completed/failed]
        
        A10 --> A19([报告生成完成])
        A11 --> A19
        A18 --> A19
    end

    style A1 fill:#e1f5fe
    style A19 fill:#c8e6c9
```

## 2. Allure报告生成流程

```mermaid
flowchart TD
    subgraph Allure报告导出
        B1(["开始"]) --> B2[Controller接收请求<br/>exportAllureReport]
        B2 --> B3[AllureReportService.exportAllureReport]
        
        B3 --> B4[buildAllureReportData<br/>构建Allure报告数据]
        B4 --> B5[查询报告基础信息<br/>selectReportExportData]
        
        B5 --> B6[buildTestSuites<br/>构建测试套件列表]
        B6 --> B7[buildTestCaseDetails<br/>构建用例详情]
        
        B7 --> B8[buildTestSteps<br/>构建测试步骤]
        B8 --> B9[buildHttpRequest/Response<br/>构建HTTP请求响应]
        
        B9 --> B10[buildAssertions<br/>构建断言结果]
        B10 --> B11[buildAttachments<br/>构建附件信息]
        
        B11 --> B12[buildModuleStatistics<br/>构建模块统计]
        B12 --> B13[buildHistoryTrends<br/>构建历史趋势]
        
        B13 --> B14[AllureHTMLBuilder.build<br/>生成Allure格式HTML]
        B14 --> B15[返回ZIP压缩包<br/>Allure报告]
        B15 --> B16([结束])
    end

    style B1 fill:#e1f5fe
    style B16 fill:#c8e6c9
```

## 3. 企业报告生成流程（多格式导出）

```mermaid
flowchart TD
    subgraph 企业报告导出
        C1(["开始"]) --> C2[Controller接收请求<br/>/api/reports/{id}/export]
        C2 --> C3[ReportExportService.exportReport]
        
        C3 --> C4[validateExportQuery<br/>校验导出参数]
        C4 --> C5[validateReportForExport<br/>验证报告状态]
        
        C5 --> C6[getReportExportData<br/>获取导出数据]
        C6 --> C7[selectReportExportData<br/>查询报告基本信息]
        C7 --> C8[selectReportStatistics<br/>查询统计数据]
        
        C8 --> C9{是否包含详情?}
        C9 -->|是| C10[selectReportTestResults<br/>查询测试结果详情]
        C9 -->|否| C11
        
        C10 --> C11[构建ExportMetadata<br/>导出元数据]
        C11 --> C12[generateFileContent<br/>根据格式生成文件]
        
        C12 --> C13{导出格式}
        C13 -->|HTML| C14[HTMLTemplateBuilder.build<br/>生成HTML报告]
        C13 -->|PDF| C15[PDFBox生成PDF<br/>PDDocument]
        C13 -->|Excel| C16[生成Excel内容]
        C13 -->|CSV| C17[生成CSV内容]
        C13 -->|JSON| C18[ObjectMapper生成JSON]
        
        C14 --> C19[generateExportFileName<br/>生成文件名]
        C15 --> C19
        C16 --> C19
        C17 --> C19
        C18 --> C19
        
        C19 --> C20[返回ByteArrayResource]
        C20 --> C21([结束])
    end

    style C1 fill:#e1f5fe
    style C21 fill:#c8e6c9
```

## 4. 报告核心组件关系

```mermaid
flowchart TD
    subgraph 核心组件
        D1[TestExecutionService]
        D2[TestReportSummary]
        D3[AllureReportService]
        D4[ReportExportService]
        D5[ReportMapper]
        D6[TestExecutionMapper]
    end

    subgraph 数据表
        D7[TestReportSummary表]
        D8[TestCaseResult表]
        D9[TestExecutionRecord表]
    end

    D1 -->|生成报告| D2
    D2 --> D7
    D6 -->|保存用例结果| D8
    D8 --> D9
    D9 --> D2
    D5 -->|查询报告| D7
    D3 --> D5
    D4 --> D5
```

## 5. 报告数据结构

```mermaid
flowchart TD
    subgraph TestReportSummary
        E1[reportId报告ID]
        E2[reportName报告名称]
        E3[reportType报告类型]
        E4[totalCases总用例数]
        E5[passedCases通过数]
        E6[failedCases失败数]
        E7[brokenCases异常数]
        E8[skippedCases跳过数]
        E9[successRate成功率]
        E10[duration执行耗时]
        E11[reportStatus状态]
    end

    subgraph TestCaseResult
        F1[executionId执行ID]
        F2[caseId用例ID]
        F3[status状态]
        F4[responseStatus响应状态码]
        F5[duration耗时]
        F6[failureMessage失败信息]
        F7[request请求信息]
        F8[response响应信息]
    end
```

## 流程说明

### 1. 测试执行中报告生成
- **单用例执行**：执行完成后调用 `generateTestReport()` 生成独立报告
- **批量/模块/项目执行**：先创建报告摘要记录（状态=generating），执行过程中记录每个用例结果，全部完成后更新统计信息

### 2. Allure报告导出
- 调用 `AllureReportService.exportAllureReport()`
- 构建 Allure 格式的数据结构（测试套件、用例、步骤、HTTP请求响应等）
- 使用 `AllureHTMLBuilder` 生成 Allure 风格的 HTML 报告
- 返回 ZIP 压缩包格式

### 3. 企业报告导出
- 支持多种格式：HTML、PDF、Excel、CSV、JSON
- **HTML**：使用 `HTMLTemplateBuilder` 生成，包含图表（Chart.js）
- **PDF**：使用 PDFBox 库生成，支持中文
- 查询报告基础信息、统计信息、测试结果详情
- 返回 `ByteArrayResource` 供下载